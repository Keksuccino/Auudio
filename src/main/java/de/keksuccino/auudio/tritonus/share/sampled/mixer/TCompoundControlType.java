package de.keksuccino.auudio.tritonus.share.sampled.mixer;

import javax.sound.sampled.CompoundControl.Type;

public class TCompoundControlType extends Type {

   public TCompoundControlType(String strName) {
      super(strName);
   }
}
